function Header({name}) {
    return (
        <h1>{name}</h1>;
        <nav>
            <ul>
                {menu.map((item, index))} = (
            </ul>
        </nav>
         )   
         
}
export default Header;